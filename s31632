<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Constant Navigation -->
<nav class="nav">
  <div class="nav-inner">
    <a class="logo" href="Home.html">Freedomshop</a>

    <input id="menu-toggle" type="checkbox" />
    <label class="burger" for="menu-toggle">☰</label>

    <div class="links">
      <a href="Home.html">Home</a>
      <a href="Equipment.html">Equipment</a>
      <a href="Contacts.html">Contact</a>
    </div>
  </div>
</nav>

  <meta charset="UTF-8" />
  <title>Helldivers 2 — Freedomshop</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap" rel="stylesheet" />
  <style>
    :root { --bg:#1f1f1f; --card:#262626; --text:#f0f0f0; --muted:#bdbdbd; --accent:#ffd166; --line:#3a3a3a; }
    * { box-sizing:border-box; }
    body { margin:0; font-family:'Orbitron',sans-serif; background:var(--bg); color:var(--text); }
    a { color:var(--text); text-decoration:none; }
    /* NAV (constant) */
    .nav { position:sticky; top:0; z-index:10; background:rgba(0,0,0,.45); backdrop-filter:blur(6px);
           border-bottom:1px solid rgba(255,255,255,.07); }
    .nav-inner { max-width:1100px; margin:0 auto; display:flex; align-items:center; gap:16px; padding:12px 16px; }
    .logo { color:var(--accent); font-weight:700; letter-spacing:.04em; }
    .links { margin-left:auto; display:flex; gap:16px; }
    .links a.active { color:var(--accent); }
    /* MOBILE: simple burger (no JS) */
    #menu-toggle { display:none; }
    .burger { display:none; margin-left:auto; font-size:22px; cursor:pointer; }
    @media (max-width: 768px) {
      .links { display:none; position:absolute; right:16px; top:56px; background:var(--card); border:1px solid var(--line);
               border-radius:12px; padding:10px; flex-direction:column; }
      .burger { display:block; }
      #menu-toggle:checked + label + .links { display:flex; }
    }

    .container { max-width:1100px; margin:24px auto; padding:0 16px; }
    .card { background:var(--card); border:1px solid var(--line); border-radius:12px; padding:16px; }
    h1, h2 { color:var(--accent); letter-spacing:.04em; text-transform:uppercase; margin:.4em 0; }
    p { line-height:1.45; color:var(--text); }
    .muted { color:var(--muted); }
  </style>
</head>
<body>

  <main class="container">
    <section class="card">
      <h1>Welcome to My Freedomshop.com</h1>
      <p class="muted">
        Greetings, citizens of <strong>Super Earth</strong>! The Ministry of Truth has authorized us
        to sell battle-ready equipment. We’ll be adding new items soon — for now, enjoy your reservations
        in honor of <strong>Super Earth Day</strong>!
      </p>
    </section>

    <section class="card">
      <h2>About the Game</h2>
      <p>
        Helldivers 2 is a co-op shooter where squads deliver managed democracy across the galaxy.
        Suit up, pick stratagems, and complete operations for the glory of Super Earth.
      </p>
     
  <footer class="container muted" id="contact" style="text-align:right; padding-bottom:24px;">
    Illia Yermakov · <a href="mailto:s31632@pjwstk.edu.pl" style="color:var(--accent);">s31632@pjwstk.edu.pl</a>
  </footer>
</body>
</html>
